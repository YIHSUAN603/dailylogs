# Changelog

本檔案記錄本專案各版本的重要變更，格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)。

## [2.4.0] - 2026-07-02

### 新增

- **AI 逾時設定**：設定頁 AI 區段新增逾時秒數欄位（5–3600 秒，留空重設為 120），測試連線時同樣套用。
- **日報範本**：設定頁可自訂範本；開啟尚無日報的日期時自動以範本預填，未編輯不會建檔。
- **帶入昨日**：編輯器工具列新增「帶入昨日」，自動擷取前一份日報的進行中/明日/待辦段落，附加為「## 承上日」區塊；無命中時可選擇帶入整份，並防止重覆帶入。
- **PAT 改存作業系統 keychain**：TFS PAT 優先存 Windows 憑證管理員 / Linux Secret Service（不可用時退回本機資料庫），舊明文會自動搬移；匯出備份一律排除 PAT。

### 修正

- **AI 結果誤寫他日**：AI 整理完成時若已切換日期，結果會被丟棄，不再寫進其他日期的日報。
- **切換日期遺失編輯**：切日前會先寫入未觸發的防抖存檔，刪除日報前丟棄該日暫存，避免遺失最後 0.6 秒的編輯。
- **匯入備份半途失敗留下殘料**：匯入改包在單一交易，中途失敗整批回滾。
- **週報彙整名實不符**：手動調整起迄日後改列「自訂區間」，不再沿用「週報/月報」標題，並擋下起日晚於迄日的查詢。
- **測試連線偷偷存檔**：AI 與 TFS 的「測試連線」改為暫存輸入→測試→還原，按測試不再等於儲存設定。
- **AI 整理 prompt 重複**：移除 raw_notes 重複餵入的區塊，並統一四面向名稱為「待辦」。

### 變更

- 全域提示統一改用 toast，取代 16 處阻塞式 alert 與內嵌錯誤訊息。
- 工作面板編輯彈窗在有未儲存變更時，點背景或 Esc 會先確認再關閉。
- 專案進度彙整結果改用 Markdown 渲染，與週報呈現一致。
- 匯入備份後即時重套主題設定（主色/深淺模式），不需重啟。

### 安全性

- 啟用 WebView 內容安全政策（CSP）：script 僅允許 self、連線僅限 Tauri IPC，降低 XSS 影響；圖片開放 http/https/data/blob 供 Markdown 預覽使用。

### 其他

- 新增 TFS 過濾、AI 回應解析、日期工具、資料庫 CRUD/匯出入等單元測試。
- 新增使用說明書（Markdown/Word）與各功能操作截圖，補充 AI 命令 CLI 範例與常見問題。
- 統一 Date 轉 YYYY-MM-DD 為共用 `dateStr`，移除未使用的型別與 command。
